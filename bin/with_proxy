#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail
(shopt -p inherit_errexit &>/dev/null) && shopt -s inherit_errexit

socket_addr=socks5://127.0.0.1:1080
http_addr=http://127.0.0.1:8001
HTTP_PROXY=$http_addr HTTPS_PROXY=$http_addr ALL_PROXY=$socket_addr "$@"
